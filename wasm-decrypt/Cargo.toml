[package]
name = "wasm-decrypt"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
aes-gcm = "0.10.3"
hex = "0.4.3"
getrandom = { version = "0.2", features = ["js"] }
zeroize = { version = "1.8", features = ["zeroize_derive"] }
